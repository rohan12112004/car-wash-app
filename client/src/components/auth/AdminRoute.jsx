/**
 * @file AdminRoute.jsx
 * @description Direct pass-through wrapper for AdminDashboardPage which manages its own Founder Verification Gate.
 */
import React from 'react';

const AdminRoute = ({ children }) => {
  return children;
};

export default AdminRoute;
